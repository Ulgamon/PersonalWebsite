import { Element } from "react-scroll";
const BlogsSection = () => {
  return (
    <Element name="blogs">
      <section id="blogs" className="min-h-screen">
        Blogs Section
      </section>
    </Element>
  );
};

export default BlogsSection;
