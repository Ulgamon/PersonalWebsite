import { Element } from "react-scroll";

const ServicesSection = () => {
  return (
    <Element className="max-w-screen-customMaxWidth mx-auto" name="services">
      <section id="services" className="min-h-screen">
        Services Section
      </section>
    </Element>
  );
};

export default ServicesSection;
