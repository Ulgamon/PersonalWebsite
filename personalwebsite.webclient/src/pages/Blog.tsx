import AppOutlet from "@/components/app-outlet/AppOutlet";

const Blog = () => {
  return (
    <AppOutlet>
      <p className="w-full min-h-screen text-black text-4xl dark:text-white">
        BLOG PAGE that will display blog. this is the last thing to be done
      </p>
    </AppOutlet>
  );
};

export default Blog;
